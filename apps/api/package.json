{
  "name": "@dialbrio/api",
  "version": "0.0.0",
  "private": true,
  "description": "NestJS modular monolith (Phase 1). The Prisma schema defines the canonical data model.",
  "scripts": {
    "prisma:validate": "prisma validate",
    "prisma:format": "prisma format"
  },
  "devDependencies": {
    "prisma": "6.19.3"
  }
}
